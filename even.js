const isEven = function(num) {
  return num % 2 === 0;
}

/* 
Can be omitted to simplify code
We don't need to set our function return values to new variables before doing something with them

const tenIsEven = isEven(10);
const elevenIsEven = isEven(11);
*/

console.log(isEven(10));
console.log(isEven(11));